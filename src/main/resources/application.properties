#Database
spring.datasource.url = jdbc:mariadb://localhost:3306/stock?createDatabaseIfNotExist=true
	spring.datasource.username = root
	
	#code mysql
	spring.datasource.password = fms2023
	spring.datasource.driver-class-name = org.mariadb.jdbc.Driver
	
	#Jpa-Hibernate
	Spring.jpa.show-sql =true
	
	#En cours de réalisation il continue à implementer la base de donner
	Spring.jpa.hibernate.ddl-auto = update
	
	#Il reinitialise la base de donner à chaque requête
	#Spring.jpa.hibernate.ddl-auto = create
	Spring.jpa.properties.hibernate.dialect = org.hibernate.dialect.MariaDB53Dialect